# Property-Based Tests for Aletheia

> Auto-generated by Aletheia - MoonBit PBT Tool

## Overview

- **Source**: `src/dogfooding`
- **Generated**: 2026-01-14
- **Patterns Detected**: 4

## Dogfooding（自己テスト）

このモジュールはAletheiaツール自身にプロパティベーステストを適用し、ツールの品質を自己検証します。

### テストサイクル

1. **関数抽出**: `get_aletheia_functions()` でツール自身の公開関数リストを取得
2. **パターン検出**: `detect_self_patterns()` でRound-Trip、Idempotent、Producer-Consumerパターンを検出
3. **テスト生成**: `generate_self_pbt_md()` でプロパティテストを生成
4. **検証**: `moon test` で生成されたテストを実行

### 実行方法

```bash
# 全テスト実行
moon test

# dogfoodingモジュールのみ
moon test src/dogfooding
```

## Idempotent Properties

### `sort` Idempotence

Idempotent property: applying function twice should be same as applying once.

```mbt
///|
test "prop_sort_idempotent_demo" {
  let input = [3, 1, 2]
  inspect(input.length(), content="3")
}
```
